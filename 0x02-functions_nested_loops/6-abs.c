#include "main.h"
/**
 * _abs - computes the ab value of int
 * Return: Always 0
 * @n: the int in question
 * return: the abs of the int.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

