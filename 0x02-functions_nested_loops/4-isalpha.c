#include "main.h"
/**
 * _isalpha - checks for char a alphabet
 * @c: the char to be checked
 * Return: 1 if chae is letter lower or upper, 0 otherwise
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && <= 'z'))
		return (0);
	else
		return (0);	
}
	
